# Results

_Not yet populated._

This file is generated by `python -m nuvla_forge.bench.report` from the JSON in
`reports/`. It stays empty until the sweep runs on a real, probed machine:

```bash
bash scripts/probe_hardware.sh
bash scripts/run_all_benchmarks.sh
```

Numbers that were not measured on named hardware are decoration, so none are
published here in advance.
